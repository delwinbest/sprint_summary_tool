name: tests-sprints

on:
  push:
    branches:
      - master
    paths:
      - 'sprints/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd sprints && npm install && npm run test:ci
