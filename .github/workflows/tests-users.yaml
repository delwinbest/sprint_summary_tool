name: tests-users

on:
  pull_request:
    paths:
      - 'users/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Run Tests
        run: cd users && npm install && npm run test:ci
