name: tests-teams

on:
  pull_request:
    paths:
      - 'teams/**'
  push:
    paths:
      - 'teams/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd teams && npm install && npm run test:ci
